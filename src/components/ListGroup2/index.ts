import ListGroup from "./ListGroup2";

export default ListGroup;

// In the main App.tsx file, we'll import the folder ListGroup
// and it will automatically import the index.ts file where
// we have the ListGroup component.
